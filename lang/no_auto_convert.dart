/// Shows an assigment most would expect to work, commented out
/// so that this project will compile. Un-comment the assigment
/// to watch dart failure in action.
main() {

	int i = 1;
	double d = 0;
	// d = i;		// FAIL - A value of type 'int' can't 
	//		// be assigned to a variable of type 'double'
	print(d);
}
