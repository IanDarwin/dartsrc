/// These are examples of what NOT to do, just showing
/// that weird things are sometimes allowed.
main() {
	throw DateTime.now(); // Do not do this!
	throw 1;              // Don't do this either!
}
